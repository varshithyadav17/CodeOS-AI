# ============================================================
# Standalone production requirements — no private packages.
# Install with:  pip install -r requirements.txt
# Run with:     uvicorn server:app --reload --port 8001
# ============================================================

# --- Web framework -----------------------------------------------------
fastapi==0.110.1
uvicorn[standard]==0.25.0
python-multipart>=0.0.9
starlette>=0.36.0

# --- Database ----------------------------------------------------------
motor==3.3.1
pymongo==4.6.3

# --- Configuration / env ----------------------------------------------
python-dotenv>=1.0.1

# --- Validation --------------------------------------------------------
pydantic>=2.6.4
email-validator>=2.2.0
tzdata>=2024.2

# --- Auth (JWT + password hashing) -------------------------------------
# Only pyjwt is imported anywhere in app/ (see app/core/security.py);
# python-jose was an unused duplicate JWT implementation.
pyjwt>=2.10.1
passlib[bcrypt]>=1.7.4
bcrypt==4.1.3
cryptography>=42.0.8

# --- HTTP client (test tooling only) ------------------------------------
# `requests` is used by the integration test scripts in backend/tests/.
# httpx and requests-oauthlib were unused (no import of either anywhere
# in app/ or tests/) and have been removed.
requests>=2.31.0

# --- AI / LLM (standalone Google Gemini SDK) --------------------------
# Replaces the previous third-party LLM wrapper. Provide GEMINI_API_KEY
# in backend/.env to enable AI chat, code review and documentation
# generation.
google-genai>=0.3.0

# --- Repository ingestion / parsing -----------------------------------
GitPython>=3.1.43
# Pin tree-sitter to the 0.21.x line that matches the binary ABI of
# `tree-sitter-languages` 1.10.2. Newer tree-sitter (0.22+) introduces a
# Parser API change that breaks `get_parser()` from tree-sitter-languages.
tree-sitter==0.21.3
tree-sitter-languages==1.10.2

# --- Dev / test tooling ------------------------------------------------
pytest>=8.0.0
pytest-asyncio>=0.23.6
pytest-xdist>=3.6.0
black>=24.1.1
isort>=5.13.2
flake8>=7.0.0
mypy>=1.8.0
